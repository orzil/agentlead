httpx>=0.27
beautifulsoup4>=4.12
rapidfuzz>=3.6

# optional - only if you configure Reddit API credentials (RSS works without):
praw>=7.7

# optional - only for the Facebook public-group scraper (FB_PUBLIC_ENABLED=1):
# after installing, also run: playwright install chromium
playwright>=1.44

# WhatsApp reader (optional; only needed on the machine running whatsapp_reader.py)
neonize>=0.4.3
