# secrets & local state — NEVER commit
.env
leads.db
leads.db-*
*.log
outbox.log

# exports / scratch — lead data (incl. posters' names) stays out of git; the
# cloud run keeps leads.db in the Actions cache instead. See SETUP_CLOUD.md.
leads.html
*.csv
rc.txt
*.tmp
# the morning table IS lead data (summaries, budgets, posters' post URLs).
# The repo is public, so it goes to Telegram and stays out of git - same rule
# as leads.html and the CSVs above.
MORNING_REPORT.md

# python
__pycache__/
*.pyc
.venv/
venv/

# playwright
/ms-playwright/

# WhatsApp linked-device session - these ARE credentials
wa_session/
